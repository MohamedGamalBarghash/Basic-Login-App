import 'package:flutter/material.dart';

class Settings extends StatelessWidget
{
  Widget build(BuildContext context)
  {
    return new Container(
      child: new Column(
        children: <Widget>[
          new Text("Thanks for testing my basic login app",style: TextStyle(fontSize: 20, fontStyle: FontStyle.normal,),),
        ],
      ),
    );
  }
}